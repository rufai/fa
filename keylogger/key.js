document.addEventListener("keydown", (e) => {
    console.log(e.key)
    if( e.key === "y" ){
        alert("You press y")
    }
    if( e.key === "c" ){
        copy()
    }
})

function copy(){

}

function paste(){

}